### 系统配置整合与文档长度限制优化（2026-04-08 17:40）

28. **发现并修复重复配置系统问题**
    - **原问题**: 系统已有 `/admin/system_config` 路由和配置管理器 (`config_manager.py`)，但之前创建了重复系统（已被删除）
    - **清理冲突代码**:
      - 删除 `system_config_manager.py`
      - 删除 `app.py:1931-1998` 的重复路由
      - 删除重复的导入：`from system_config_manager import config_manager as sys_config_manager`
    
29. **文档提取长度限制优化（基于现有系统）**
    - **需求**: 取消上传文档时的文本长度限制，或在系统设置中可配置
    - **解决方案**:
      1. **扩展现有配置管理器** (`config_manager.py`):
         - 在 `DEFAULT_CONFIG` 中添加 `document` 章节
         - 包含 `max_extracted_length`, `max_preview_length`, `max_ai_sample_length`, `max_file_preview_length`, `max_upload_size_mb`
      2. **更新 `init_config.py`**: 添加相同的默认配置
      3. **修复硬编码限制** (`app.py`):
         - 将 `Config.MAX_AI_SAMPLE_LENGTH` 等替换为 `config_manager.get("document.max_ai_sample_length", 5000)`
         - 支持 `-1` 表示无限制
      4. **更新 `utils.py`**: `extract_file_content()` 使用 `config_manager.get("document.max_extracted_length", -1)`
      5. **增强系统配置页面**:
         - 路由 `/admin/system_config` 添加 `document` 分类配置项
         - 配置项使用点分隔键名（如 `document.max_extracted_length`）
         - 更新前端JS适配现有的 `/admin/system_config/save` 路由
    
30. **系统配置页面兼容性修复**
    - **问题**: 模板期望 `categories` 字典，路由返回 `config` 字典
    - **修复**: 在路由中添加转换逻辑，将 `config` 数据转换为模板期望的 `categories` 格式
    - **配置项格式**: 
      ```python
      categories = {
        "document": [
          {"key": "document.max_extracted_length", "type": "integer", 
           "value": -1, "description": "最大提取长度(-1无限制)", ...},
          # ...
        ]
      }
      ```
    - **JS适配**: 更新 `saveConfig()` 函数解析点分隔键名为 `section` 和 `field`，匹配现有保存路由
    
31. **验证与测试**
    - **单元测试**: 配置管理器功能完善，支持动态获取/更新配置
    - **路由测试**: 系统配置路由正常返回分类数据
    - **应用测试**: 文档提取长度配置可动态调整
    - **向后兼容**: `config.py` 保留原常量作为默认回退值
    
32. **关键配置项（document章节）**:
    - `max_extracted_length`: -1（默认无限制）
    - `max_preview_length`: 100000（预览页面）
    - `max_ai_sample_length`: 5000（AI分析）
    - `max_file_preview_length`: 50000（文件预览）
    - `max_upload_size_mb`: 1024（1GB）