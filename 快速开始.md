# 智能服务办公平台 — 快速开始指南

> 版本 2.0 | 2026-07-26

## 环境要求

- Windows 10/11 或 Linux/macOS
- Python 3.8+
- 现代浏览器（Chrome/Edge/Firefox）
- LibreOffice（可选，用于文档转换）

---

## 快速启动

### 方式一：一键启动（Windows）

双击 `启动系统.bat`，自动完成初始化并启动服务。

首次使用先运行 `初始化系统.bat`。

### 方式二：命令行

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env，至少设置 SECRET_KEY

# 3. 初始化数据库（首次）
python init_database.py

# 4. 启动
python app.py
```

访问 `http://127.0.0.1:5000`

---

## 首次登录

启动后控制台输出：

```
============================================================
  管理员账号已创建
  用户名: admin
  初始密码: X7kQm2abcd1234ef（随机生成）
  请登录后立即修改密码！
============================================================
```

> ⚠️ **安全提示**：初始密码为随机生成，**仅此一次显示**。如需自定义密码，在 `.env` 中设置：
> ```ini
> ADMIN_INITIAL_PASSWORD=mypassword
> ```

---

## 环境变量参考

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `SECRET_KEY` | Flask Session 签名密钥 | 随机生成(每次重启变化) |
| `API_ENCRYPTION_KEY` | AI API Key 加密密钥 | 回退用 SECRET_KEY |
| `ADMIN_INITIAL_PASSWORD` | 首次初始化管理员密码 | 随机 16 位 |
| `DATABASE_URL` | 数据库连接串 | `sqlite:///oa.db` |
| `FLASK_PORT` | 监听端口 | `5000` |
| `FLASK_HOST` | 监听地址 | `127.0.0.1` |
| `FLASK_DEBUG` | 调试模式 | `false` |
| `SOFFICE_PATH` | LibreOffice 路径 | 自动搜索 |
| `LOG_LEVEL` | 日志级别 | `INFO` |
| `SESSION_LIFETIME` | Session 有效期(秒) | `604800` (7天) |
| `MAX_CONTENT_LENGTH` | 上传文件大小上限(字节) | `1073741824` (1GB) |

---

## 系统初始化内容

首次运行自动创建以下数据：

### 组织架构

| 机构 | 部门 |
|------|------|
| 智能服务办公平台 (ROOT) | 办公室、人事部门、财务部门、业务部门、信息技术部 |

### 系统角色

- 系统管理员 — 全部权限
- 部门经理 — 部门管理 + 统计
- 信息报送员 — 报告上报
- 普通员工 — 基础功能

### 内置模板

请示、报告、通知、函、会议纪要共 5 种公文模板。

### 简报数据源

人民日报、新华网、央视新闻、中国政府网、光明日报。

### 会议室

第一会议室(20人)、第二会议室(10人)、多功能厅(50人)、视频会议室(15人)。

### 档案示例

- 1 个全宗（达天科技有限公司）
- 5 个目录（文书/科技/会计/人事/合同）
- 年度子目录（2022/2023/2024）
- 10 份示例档案文件

---

## 数据库表概览

| 模块 | 表名 | 说明 |
|------|------|------|
| 用户 | `users` | 用户账户 |
| 权限 | `roles` | 系统角色 |
| 组织 | `organizations`, `departments`, `positions` | 组织架构 |
| 知识库 | `knowledge_bases`, `knowledge_files` | 知识管理 |
| 公文 | `official_docs`, `doc_flows`, `doc_read_records` | 电子公文 |
| 模板 | `doc_templates` | 公文模板 |
| 简报 | `briefings`, `briefing_articles`, `briefing_sources` | 简报系统 |
| 监测 | `url_libraries`, `url_items`, `monitor_results` | 栏目监测 |
| 爬虫 | `crawler_tasks`, `crawler_pages` | 网站采集 |
| 档案 | `archive_fonds`, `archive_catalogs`, `archive_volumes`, `archive_files` | 档案管理 |
| 会议 | `meetings`, `meeting_rooms`, `meeting_attendances` | 会议管理 |
| 督查 | `supervision_tasks`, `supervision_progresses` | 督查督办 |
| 绩效 | `performance_periods`, `performance_assessments` | 绩效考核 |
| 日志 | `work_logs`, `work_log_reviews` | 工作日志 |
| 配置 | `system_configs` | 系统参数 |
| 审计 | `system_operation_logs` | 操作审计 |
| AI | `ai_model_configs`, `chat_sessions`, `chat_messages` | AI 对话 |
| FTS | `knowledge_files_fts` | 全文搜索索引 |

---

## 重置系统

```bash
# 停止服务 → 删除数据库文件
del oa.db
# 重新启动
python app.py
```

---

## 生产部署

详见 [部署说明.md](部署说明.md)

```bash
# 打包为可执行文件
打包部署.bat
# 输出在 dist/ 目录
```
