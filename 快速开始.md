# 智能服务办公平台 - 快速开始指南

## 首次使用

### 方法一: 一键初始化 (推荐, Windows用户)

1. **双击运行** `初始化系统.bat`
   - 自动创建必要目录
   - 生成配置文件
   - 初始化数据库和默认数据
   - 检查依赖包

2. **双击运行** `启动系统.bat`
   - 启动Web服务
   - 访问 http://127.0.0.1:5000

3. **登录系统**
   - 用户名: `admin`
   - 密码: `admin123`
   - ⚠️ 登录后请立即修改默认密码!

### 方法二: 手动初始化

1. **创建环境和目录**
   ```bash
   python init_project.py
   ```

2. **初始化数据库**
   ```bash
   python init_database.py
   ```

3. **启动系统**
   ```bash
   python app.py
   ```

## 系统初始化内容

### 数据库表结构
- 用户管理 (users)
- 角色权限 (roles)
- 组织架构 (organizations, departments, positions)
- 知识库 (knowledge_bases, knowledge_files)
- 公文管理 (official_docs, doc_templates)
- 档案管理 (archive_fonds, archive_catalogs, archive_volumes, archive_files)
- 会议管理 (meeting_rooms, meetings)
- 简报系统 (briefing_sources, briefing_keywords, briefings)
- 爬虫模块 (crawler_tasks, crawler_pages)
- 督查督办 (supervision_tasks)
- 绩效管理 (performance_periods)
- 工作日志 (work_logs)
- 系统配置 (system_configs)
- 操作日志 (system_operation_logs)

### 默认数据

#### 1. 管理员账号
- 用户名: `admin`
- 密码: `admin123`
- 角色: 系统管理员

#### 2. 组织架构
- **机构**: 智能服务办公平台 (ROOT)
- **部门**:
  - 办公室 (OFFICE)
  - 人事部门 (HR)
  - 财务部门 (FINANCE)
  - 业务部门 (BUSINESS)
  - 信息技术部 (IT)
- **岗位**: 系统管理员、办公室主任、文秘、人事主管、业务经理等

#### 3. 系统角色
- 系统管理员 (拥有所有权限)
- 部门经理
- 信息报送员
- 普通员工

#### 4. 公文模板
- 请示(标准格式)
- 报告(工作总结)
- 通知(标准格式)
- 函(商洽事项)
- 会议纪要

#### 5. 简报数据源
- 人民日报、新华网、央视新闻、中国政府网、光明日报

#### 6. 会议室
- 第一会议室 (3楼301, 20人)
- 第二会议室 (3楼302, 10人)
- 多功能厅 (4楼401, 50人)
- 视频会议室 (4楼402, 15人)

#### 7. 档案示例数据
- 全宗: 达天科技有限公司 (DATIAN)
- 目录: 文书档案、科技档案、会计档案、人事档案、合同档案
- 案卷: 综合管理类、人事劳资类等
- 示例档案文件10份

#### 8. 系统配置
- 系统名称、文件大小限制、AI参数等

## 目录结构

```
rooa/
├── app.py                    # 主应用入口
├── config.py                 # 配置文件
├── models.py                 # 数据模型
├── archive_models.py         # 档案模块模型
├── init_system.py           # 系统完整初始化脚本
├── init_database.py         # 数据库初始化脚本
├── init_project.py          # 项目基础初始化
├── init_config.py           # 配置文件初始化
├── init_archive_data.py     # 档案数据初始化
├── 初始化系统.bat           # Windows一键初始化
├── 启动系统.bat             # Windows启动脚本
├── config/                  # 配置目录
│   └── system_config.json
├── uploads/                 # 上传文件目录
│   ├── knowledge/
│   ├── archive/
│   └── ...
├── templates/               # 模板文件
└── static/                  # 静态资源
```

## 常见问题

### Q: 数据库初始化失败怎么办?
A: 
1. 检查Python版本 >= 3.7
2. 安装依赖: `pip install -r requirements.txt`
3. 删除oa.db文件重新初始化

### Q: 如何重置数据库?
A:
1. 停止服务
2. 删除 `oa.db` 文件
3. 重新运行初始化脚本

### Q: 如何修改默认端口?
A:
编辑 `app.py` 文件最后的运行代码,修改port参数:
```python
if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000, debug=True)
```

### Q: 如何备份数据库?
A:
直接复制 `oa.db` 文件即可。SQLite数据库是单个文件。

## 技术支持

如遇问题,请检查:
1. Python版本和依赖包
2. 端口5000是否被占用
3. 目录读写权限
4. 数据库文件完整性
