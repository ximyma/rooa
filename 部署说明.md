# 智能服务办公平台 - 生产部署指南

## 目录

1. [概述](#概述)
2. [准备工作](#准备工作)
3. [一键打包流程（强烈推荐！）](#一键打包流程强烈推荐)
4. [虚拟环境说明](#虚拟环境说明)
5. [测试打包结果](#测试打包结果)
6. [分发给客户](#分发给客户)
7. [安全建议](#安全建议)
8. [常见问题](#常见问题)

---

## 概述

本指南将帮助您把智能服务办公平台打包成 Windows 可执行文件（.exe），
并提供完整的部署流程说明。

### 特点

- **源代码保护**：通过 PyInstaller 打包，源代码编译成 bytecode
- **一键启动**：客户双击 .exe 即可运行
- **自动初始化**：首次运行会自动创建数据库
- **便捷部署**：不需要安装 Python，解压即用
- **虚拟环境隔离**：打包使用独立虚拟环境，确保结果可靠

---

## 准备工作

### 1. 环境要求

**开发环境**

- Windows 操作系统
- Python 3.7 或更高版本

**（打包脚本会自动安装依赖包！）**

### 2. 检查项目结构

确保以下关键文件存在：

```
rooa/
├── app.py                  # 主程序
├── models.py               # 数据模型
├── archive_models.py      # 档案模型
├── init_db_standalone_simple.py  # 数据库初始化
├── build_exe.py           # 打包脚本
├── 打包部署.bat           # ⭐ 一键打包（推荐）
├── requirements.txt       # 依赖列表（已更新）
├── templates/             # HTML 模板（必须！）
├── static/                # 静态资源（必须！）
└── config/                # 配置文件（可选）
```

---

## 一键打包流程（强烈推荐！）

### 🎯 方式一：使用批处理脚本（最简单！）

**零配置！双击打包！**

1. **双击运行 **打包部署.bat**

脚本会自动完成：
- ✅ 检查 Python 环境
- ✅ 创建独立的虚拟环境 `venv_build`
- ✅ 激活虚拟环境
- ✅ 更新 pip 并安装所有依赖包
- ✅ 检查项目文件完整性
- ✅ 执行 PyInstaller 打包
- ✅ 准备分发包
- ✅ 完成后可选择打开输出目录

**如果您使用 PowerShell，也可以运行：**
```powershell
.\打包部署.ps1
```

（如需强制重建虚拟环境：
```powershell
.\打包部署.ps1 -CleanVenv
```
）

### 📦 打包输出目录 `dist/`

打包完成后，检查结果：

```text
dist/
├── 智能服务办公平台.exe  ← 主程序，双击运行！
├── README.txt
├── 备份数据库.bat
├── templates/
├── static/
├── config/
└── uploads/...
```

---

## 虚拟环境说明

### 为什么使用虚拟环境？

虚拟环境的好处：
- 🛡️ **隔离**：不影响系统环境或其他项目
- 📦 **确定**：确保依赖包版本完全一致
- 🧹 **干净**：只打包必要的依赖，控制体积
- 🔄 **可重现**：不同机器上打包结果一致

### 虚拟环境目录

脚本会在项目目录下创建 `venv_build` 文件夹：
- 这是临时的打包专用虚拟环境
- 打包过程使用
- 打包完成后可以安全删除（如果不打算再打包）
- 如需重新打包，脚本会自动检测/重建

---

## 测试打包结果

### 1. 检查输出文件

打包完成后，确认 `dist/` 目录中有完整文件：

- [ ] `智能服务办公平台.exe` - 存在
- [ ] `templates/` - 已复制
- [ ] `static/` - 已复制
- [ ] `config/` - 已创建
- [ ] `uploads/` - 已创建（各子目录）
- [ ] `README.txt` - 已创建
- [ ] `备份数据库.bat` - 已创建

### 2. 测试程序运行

**重要！在分发前必须测试！**

1. 将 `dist/` 文件夹整个复制到一个临时位置（比如桌面）
2. 双击运行 **智能服务办公平台.exe**
3. 观察控制台输出，应该看到：
   - 程序自动检测数据库不存在
   - 开始初始化数据库
   - 浏览器自动打开
   - 地址栏显示 `http://127.0.0.1:5000`
4. 使用 `admin` / `admin123` 登录
5. 测试各项功能是否正常

### 3. 验证数据库

首次运行会在 dist/ 目录下自动创建 `oa.db`。

---

## 分发给客户

### 1. 压缩包准备

#### Windows 自带压缩工具：

1. 右键点击 `dist/` 目录
2. 选择"发送到 -> 压缩(zipped)文件夹"
3. 或者使用 7-Zip 或 WinRAR 压缩（推荐）

**命名建议：**

`智能服务办公平台_v1.0_20260501.zip`

### 2. 客户安装

**零安装！解压即用**

1. 把 Zip 给客户，解压到任意目录（如 `D:\OA_System`）
2. 解压后直接运行 **智能服务办公平台.exe**

### 3. 客户使用说明

为客户提供简单的操作指南（可包含在邮件中）：

```
智能服务办公平台 - 快速开始
-----------------------------

1. 首次运行：双击 `智能服务办公平台.exe`
2. 等待数据库自动初始化
3. 浏览器会自动打开
4. 登录系统：
   用户名：admin
   密码：  admin123
5. ⚠️ 首次登录后请立即修改密码！

常见问题
----------
- 无法启动？检查 5000 端口是否被占用
- 数据丢失？删除同目录下的 oa.db 重新初始化
- 备份数据？运行同目录下的"备份数据库.bat"
```

---

## 安全建议

### 1. 源代码保护

打包已通过以下方式保护：
- PyInstaller 把 .py 编译成 Python bytecode
- 用户无法获取原始源代码
- 客户只接触到可执行文件

### 2. 数据库安全

- SQLite 数据库 `oa.db` 是单个文件
- 定期使用“备份数据库.bat”备份
- 确保数据库文件有合适的文件权限

### 3. 访问控制

- 防火墙允许 5000 端口
- 局域网访问：http://[您的IP]:5000
- 建议修改默认密码
- 考虑配置合适的用户权限

### 4. 敏感配置

修改默认配置文件：

- config/system_config.json
- 可以调整文件大小限制
- 配置 AI 参数
- 调整安全选项

---

## 常见问题

### 打包相关问题

**Q：打包后文件太大？**
A：通常是正常的，包含完整 Python 解释器，一般 30~80 MB。
可以尝试使用 UPX 压缩（已在脚本中配置）。

**Q：打包失败？**
A：
- 检查是否所有依赖都通过虚拟环境安装
- 查看错误提示信息
- 尝试删除 `venv_build` 目录重新运行
- 临时关闭杀毒软件，或添加白名单
- 确认有足够的磁盘空间

**Q：虚拟环境创建失败？**
A：
- 确保 Python 版本 >= 3.7
- 检查系统是否有创建目录的权限
- 可以手动创建：`python -m venv venv_build`

**Q：如何完全重新打包？**
A：运行：
```
# 删除旧的虚拟环境
rmdir /s /q venv_build
# 然后重新运行
打包部署.bat
```

### 运行时问题

**Q：客户无法启动？**
A：
- 检查防火墙设置
- 确认 5000 端口没被占用
- 查看控制台输出的错误信息
- 确认文件夹有写入权限（用于创建数据库）
- 检查杀毒软件是否拦截

**Q：数据库初始化失败？**
A：
- 确保 dist/ 文件夹有写入权限
- 检查是否有其他程序锁定了可能的文件
- 查看控制台错误信息

**Q：如何在多台机器上部署？**
A：
- 同一压缩包可以分发到多台 Windows 机器
- 每台机器解压到自己的目录运行
- 每个实例有独立的数据库
- 如需共享数据，需要额外的同步机制（本版本为单机模式）

---

## 附录 A：打包配置详解

### 打包配置文件 OA_System.spec

打包脚本已配置好，一般不需要修改。若有特殊需要，可以编辑：

- `hiddenimports`：添加额外的模块
- `datas`：添加额外的数据文件
- `icon`：添加程序图标

---

## 附录 B：部署检查清单

### 打包前检查
- [ ] Python 3.7+ 已安装
- [ ] 所有项目文件存在（app.py, models.py, templates, static 等）
- [ ] 先运行 `python init_db_standalone_simple.py` 测试数据库初始化
- [ ] 再运行 `python verify_data_simple.py` 验证数据完整性

### 打包后验证
- [ ] dist/ 目录生成完整
- [ ] 程序可以正常启动
- [ ] 数据库可以自动初始化
- [ ] 各项功能正常测试
- [ ] admin 登录没问题
- [ ] 防火墙配置确认
- [ ] 备份脚本正常可用

---

## 附录 C：技术支持

如有问题，请联系技术团队。

---

## 最后的检查

在分发前，再确认一遍：
- ✅ 程序测试运行正常
- ✅ 数据库自动初始化成功
- ✅ 各功能可以使用
- ✅ 默认密码已提醒客户修改
- ✅ 有备份方案
- ✅ 分发包是完整的 zip

---

**打包愉快！** 🎉
